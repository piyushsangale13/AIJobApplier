export function SettingsPage() {
  return (
    <section className="glass-panel p-6">
      <p className="text-sm uppercase tracking-[0.3em] text-slate">Settings</p>
      <h3 className="mt-3 font-display text-3xl">Configuration surface</h3>
      <p className="mt-4 text-sm leading-7 text-slate">
        This page is reserved for OpenAI model settings, browser profile selection, email
        listener configuration, and provider-specific preferences in later phases.
      </p>
    </section>
  );
}
